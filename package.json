{
  "name": "liascript-course-utils",
  "version": "1.0.0",
  "description": "Utilities for LiaScript course automation, including PDF generation and SCORM export.",
  "main": "scripts/generate-pdf.js",
  "scripts": {
    "generate-pdf": "node scripts/generate-pdf.js",
    "export-scorm": "npx @liascript/exporter Test\\ 01.md --format scorm --output liascript_course.zip"
    // ^^^ IMPORTANT: You MUST replace "npx @liascript/exporter"
    // with the actual command to run the LiaScript SCORM exporter.
    // Common possibilities:
    // 1. If it's an npm package like "@liascript/exporter": "npx @liascript/exporter"
    // 2. If it's a global CLI tool (after npm install -g <tool-name>): "liascript-cli-name"
    // 3. If it's a local script: "node path/to/exporter.js"
    // Refer to LiaScript's official documentation for the correct command.
  },
  "keywords": [
    "liascript",
    "scorm",
    "pdf",
    "github-actions"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.19.2",
    "puppeteer": "^22.12.1"
    // ^^^ IMPORTANT: If the LiaScript SCORM exporter is an npm package,
    // you MUST add it here. For example:
    // "@liascript/exporter": "latest"
    // (Replace "@liascript/exporter" with the actual package name if different)
  }
}
